
# Event Calendar TWA (Telegram Web App)

## 🚀 Деплой на Vercel

Чтобы приложение работало корректно после деплоя, выполните следующие шаги:

### 1. Настройка переменных окружения (Environment Variables)
В панели управления Vercel (Project Settings -> Environment Variables) добавьте:
- `VITE_API_KEY`: Ваш Google API Key.
- `VITE_CALENDAR_ID`: ID вашего календаря.

**Важно:** Префикс `VITE_` обязателен, чтобы сборщик прокинул эти ключи в браузерную часть приложения.

### 2. Пересборка
Если вы добавили переменные после первого деплоя, перейдите во вкладку **Deployments** в Vercel, нажмите на три точки у последнего деплоя и выберите **Redeploy**.

### 3. Ограничение API ключа
В Google Cloud Console ограничьте ключ по домену вашего приложения на Vercel, чтобы его не использовали на других ресурсах.

## 🛠 Локальный запуск
Создайте `.env` в корне:
```env
VITE_API_KEY=ваш_ключ
VITE_CALENDAR_ID=ваш_id
```


# Event Calendar TWA (Telegram Web App)

Современное веб-приложение для Telegram, которое синхронизируется с Google Календарем.

## 🚀 Деплой на Vercel и GitHub

Чтобы безопасно опубликовать проект:

### 1. Подготовка Google API
1. Получите **API Key** в [Google Cloud Console](https://console.cloud.google.com/).
2. Получите **Calendar ID** в настройках вашего Google Календаря.

### 2. Настройка на Vercel
При создании проекта в Vercel (или в настройках существующего: Settings -> Environment Variables) добавьте две переменные:
- `API_KEY`: Ваш ключ Google API.
- `CALENDAR_ID`: Идентификатор календаря (например, `xxx@group.calendar.google.com`).

### 3. Безопасность (ОБЯЗАТЕЛЬНО)
После деплоя:
1. Скопируйте URL вашего приложения (например, `my-app.vercel.app`).
2. Вернитесь в Google Cloud Console -> Credentials -> Ваш API Key.
3. В разделе **Set an application restriction** выберите **Websites**.
4. Добавьте ваш домен в список разрешенных. Это предотвратит использование вашего ключа на других сайтах.

## 🛠 Локальная разработка
Для запуска локально создайте файл `.env` в корне проекта:
```env
API_KEY=ваш_ключ
CALENDAR_ID=ваш_id_календаря
```

## ✨ Особенности
- **Безопасность**: Ключи не хранятся в коде.
- **Интерактивный календарь**: Удобный выбор месяца и подсветка игровых дней.
- **Telegram Native**: Дизайн адаптирован под Telegram, используется Haptic Feedback.
- **Google Calendar API**: Автоматическая подгрузка данных.
